<% @title = "Chat" %>

<%= render 'shared/links', object: @project %>

<div class="card-darker title-container">
  <h1 class="title">Chat</h1>

  <%= turbo_stream_from "comment" %>

  <div id="comments">
    <%= render @comments %>
  </div>

  <%= render "form", comment: @comment %>
</div>