<div id="<%= dom_id comment %>">
  <h3 class="fs-5">
    <%= profile_picture_small comment.collaborator %>
    <%= link_to comment.content, project_comments_path(comment.project), class: "fs-6 px-2 py-1 bg-dark rounded" %>
  </h3>
</div>
